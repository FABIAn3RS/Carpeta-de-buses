lista de librerias para el codigo


import tkinter as tk
import webview
from PIL import Image,ImageTk
import folium
import networkx as nx
import osmnx as ox
import webbrowser

para Recomendacion_bus:
pip install flask
pip install flask-cors
pip install requests ------ Si no funciona probar este: (python -m pip install requests)     
